import Booking from "../models/Booking";

export const bookSeat = async (req, res) => {
  const { username, email, seatNumber } = req.body;

  console.log("seat book A1");

  try {

    
    
  } catch (error) {
    
  }
};
